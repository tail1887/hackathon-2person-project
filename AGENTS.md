# 프로젝트 작업 규칙

## 0. 빠른 탐색

- [1. 프로젝트 컨텍스트](#1-프로젝트-컨텍스트)
  - [1.1 하네스의 목적과 구조화 원칙](#11-하네스의-목적과-구조화-원칙)
- [2. 문서 생성·관리 규칙](#2-문서-생성관리-규칙)
  - [2.1 문서 판단 의존성(DAG) 운영](#21-문서-판단-의존성dag-운영)
  - [2.2 개발 일지 기록](#22-개발-일지-기록)
  - [2.3 스크럼 안건 기록](#23-스크럼-안건-기록)
  - [2.4 보완계획 기록](#24-보완계획-기록)
- [3. 마일스톤 검증보고서 규칙](#3-마일스톤-검증보고서-규칙)
- [4. 의사결정 로그 하네스](#4-의사결정-로그-하네스)
  - [4.1 기록해야 하는 결정](#41-기록해야-하는-결정)
  - [4.2 기록 방법](#42-기록-방법)
  - [4.3 완료 조건](#43-완료-조건)
- [5. 일반 개발 원칙](#5-일반-개발-원칙)
  - [5.1 가정과 판단](#51-가정과-판단)
  - [5.2 최소 변경](#52-최소-변경)
  - [5.3 검증](#53-검증)

새 설계·구현 작업은 2.1의 확인 순서부터, 정책 변경은 4장의 의사결정 로그 규칙부터 확인한다.

## 1. 프로젝트 컨텍스트

- **프로젝트:** 커플들이 집착하는 AI 심판이 되었다
- **제품 한 줄:** 커플이 AI 훈수와 대국 문법으로 가볍게 대화를 시작하거나, 필요할 때 감정과 본심을 정리해 전달 가능한 말을 고르게 돕는 AI 메신저
- **핵심 가치:** 관계 문제를 미루지 않게 하는 완충 장치이면서, 커플이 재미있게 대화를 이어 갈 수 있는 게임형 대화 장치
- **확정 MVP:** 구글 간편 로그인 또는 이메일·비밀번호 회원가입 → 두 사용자 연결 → 초대 링크/6자리 코드 입장 → 실시간 대국방 → 필수 AI 훈수 → 조건부 무승부 협상 → 개인/공동 미션과 상대 확인. 카카오·네이버 로그인과 사진 첨부는 MVP 범위에서 제외한다.
- **필수 전송 규칙:** 메시지 초안 뒤 `확인`을 누르면 AI 심판이 반드시 열린다. 사용자는 초안을 수정한 뒤 다시 확인하거나, 최신 AI 심판 결과에서 `원문 전송` 또는 `추천 메시지 전송` 중 하나를 선택한다.
- **현재 단계:** M0~M3 완료. M2의 자동·통합 검증은 프로젝트 소유자 수동 테스트로 대체했고 M5에서 다시 확인한다. 현재 M4(무승부 협상·미션 완주) 페이즈 1, 협상·미션 데이터와 서버 명령 기반을 진행한다.
- **참조 문서:** 확정 결정은 `doc/1. 의사결정 로그/README.md`, 제품 방향은 `doc/0_커플들이_집착하는_AI_심판이_되었다_기획서.md`, 화면·상태·인수 기준은 `doc/1_MVP_명세.md`, 시스템·데이터 기준은 `doc/3_시스템_데이터_정의.md`, 페이즈별 인터페이스 설계는 `doc/5. 인터페이스 설계/README.md`, M1.3 기술 선택·프로젝트 뼈대는 `doc/6. 기술 스택·프로젝트 뼈대/README.md`, M2 대국방 기반 구축 계획은 `doc/7. 대국방 기반 구축/README.md`, M3 AI 대화 경험 계획은 `doc/8. AI 대화 경험 구축/README.md`, 작업 순서와 완료 기준은 `doc/2_마일스톤_계획.md`를 기준으로 한다.

### 1.1 하네스의 목적과 구조화 원칙

이 하네스의 목적은 문서를 단순히 정리하는 것이 아니라, 프로젝트 시스템 개발을 지속 가능하게 만드는 것이다. 팀원과 AI는 현재 상태, 확정된 결정, 필요한 입력, 변경 영향을 빠르게 확인하고 같은 기준으로 다음 산출물과 데이터를 추가할 수 있어야 한다.

- 지속적으로 참조·재사용·검증되는 정보는 정해진 위치, 식별자, 상태, 근거 링크를 갖춘 구조로 관리한다.
- 새 문서·데이터·설계 산출물은 기존 분류와 명명 규칙에 우선 편입한다. 기존 구조로 표현할 수 없을 때만 새 분류를 만들고, 목적·범위·생성 규칙·참조 위치를 함께 정의한다.
- 확정 정책, 제안, 검증 결과, 폐기된 결정은 상태와 권한을 구분한다. 제안이나 관찰 결과가 확정 기준처럼 사용되면 안 된다.
- 하나의 기준 정보에는 최신 정책을 판정하는 주 기준 문서를 둔다. 관련 문서는 해당 정책의 자기 범위 내 영향과 구현 기준을 구체화하고, 주 기준 문서와 서로 링크한다. 같은 내용을 맥락 없이 완전히 복제하지 않는다.
- 구조는 다음 작업자가 필요한 정보를 찾고, 새 기록을 같은 형식으로 추가하며, 변경 영향을 추적하는 데 실제로 도움이 되는 수준까지만 만든다.
- 일회성 탐색 메모·임시 작업물은 꼭 필요할 때만 남긴다. 이후 기준·재사용·검증 대상이 되면 적절한 문서 또는 데이터 구조로 승격한다.
- 구조 변경은 기존 기록의 추적성, 링크, 식별자와 DAG 판단 의존성을 깨지 않아야 한다.
- AI는 확률적 초안을 만드는 도구이며, 결과의 채택과 책임은 사람이 맡는다. 사람이 의미·표현·우선순위를 확인하는 절차는 유효한 최종 검증이다.
- 자동 검증은 기본값이 아니다. 반복 오류, 해석 차이, 또는 사람 검토 비용이 실제로 커졌을 때만 가장 작은 자동화로 승격한다.
- 자동화되지 않은 작업을 곧바로 검증 공백이나 위험으로 판단하지 않는다. 현재 단계·팀 규모·사람 검토 절차·복구 가능성을 함께 평가한다.

새로운 정보나 산출물을 만들기 전에는 이후에도 참조·재사용·검증되는지, 기존 구조와 식별자 체계 안에 넣을 수 있는지, 기준·제안·검증 중 어느 상태인지, 변경 시 어떤 하위 산출물에 영향을 주는지, 새 구조가 다음 작업의 탐색·추가·검증을 실제로 쉽게 만드는지를 확인한다.

## 2. 문서 생성·관리 규칙

- 프로젝트 문서는 원칙적으로 `doc/` 폴더에 만든다.
- 새 문서 파일명은 생성 순서에 따라 `다음번호_문서명.md`로 짓는다. 현재 가장 큰 번호보다 1 큰 번호를 사용하며, 기존 문서의 번호는 바꾸지 않는다.
- `doc/0. 스크럼 안건/`의 `README.md`와 날짜별 `YYYY-MM-DD_스크럼_안건.md`, `doc/1. 의사결정 로그/`의 `README.md`와 날짜별 `YYYY-MM-DD_의사결정_로그.md`, `doc/2. 개발일지/`의 `README.md`와 날짜별 `YYYY-MM-DD_개발일지.md`, `doc/3. 검증보고서/`의 `README.md`와 검증보고서, `doc/4. 보완 계획/`의 `README.md`·날짜별 폴더·그 안의 보완계획, `doc/5. 인터페이스 설계/`의 `README.md`와 페이즈별 설계 문서는 일반 문서 번호 규칙의 예외다.
- GitHub 이력이 버전을 관리하므로 파일명에 `V2`, `v3`, `final` 같은 버전 표기는 넣지 않는다.
- 새 문서가 기존 문서를 구체화하거나 다음 단계로 이어지면, 관련 기존 문서에서 상대 링크로 연결한다.
- 새 문서가 지속적으로 참조되는 기준 문서라면 `프로젝트 컨텍스트`의 참조 문서 목록에도 추가한다.
- 문서의 핵심 방향·범위·결정이 바뀌면 의사결정 로그 규칙을 따른다. 단순 문서 생성, 링크 수정, 파일명 정리는 로그 대상이 아니다.

### 2.1 문서 판단 의존성(DAG) 운영

프로젝트의 문서·설계·구현·검증은 서로 참조할 수 있지만, 정책과 판단의 근거 관계는 순환하지 않는 유향 비순환 그래프(DAG)로 관리한다. 하위 산출물이 상위 기준의 근거가 되거나, 서로를 확정 근거로 삼아 결정을 순환시키면 안 된다.

- 기준은 `제품 방향·의사결정 로그 → MVP 명세 → 시스템·데이터 정의 → 인터페이스 설계 → 기술 스택·프로젝트 뼈대 → 구현 → 검증` 순서로 구체화한다.
- 왼쪽 또는 상위 단계는 이후 단계의 판단 근거가 된다. 하위 단계는 선행 기준을 구현·구체화·검증할 수 있지만, 선행 기준을 임의로 변경하거나 확정할 수 없다.
- 문서 탐색을 위한 양방향 링크는 허용한다. 단, 링크가 양방향이라는 이유로 정책 근거까지 상호 의존하는 것은 아니다.
- 검증보고서와 보완계획은 기준 문서 계층 밖의 관찰·제안 노드다. 기존 기준 문서에 대한 역방향 의존성 간선을 만들지 않는다.

#### 2.1.1 작업 시작 전 확인 순서

작업 결과에 영향을 주는 선행 기준 노드를 아래 순서로 확인한다. 범위와 무관한 문서를 기계적으로 모두 읽을 필요는 없다.

1. `AGENTS.md`: 현재 단계, 작업 규칙, 참조 문서
2. `doc/2_마일스톤_계획.md`: 현재 작업이 허용된 범위와 선행 완료 조건
3. `doc/1. 의사결정 로그/README.md`: 확정 정책과 변경 이력
4. 기획서의 제품 방향: 제품 정의와 장기 방향
5. `doc/1_MVP_명세.md`: 기능 범위, 화면, 상태, 인수 기준
6. `doc/3_시스템_데이터_정의.md`: 데이터, 상태 전이, 권한, 공개 범위
7. 현재 마일스톤의 선행 페이즈 산출물: 화면·계약 등 직접 의존하는 설계
8. 최신 검증보고서와 보완계획: 미해결 위험, 제안, 재검증 조건

#### 2.1.2 문서별 권한과 기준 변경

- 의사결정 로그와 상위 기준 문서만 제품 정책·MVP 범위·기술 선택·데이터 공개 정책을 확정한다. 상세 명세와 설계 문서는 상위 기준을 구체화하고, 구현은 확정된 기준을 따른다.
- 검증보고서는 충족 여부와 위험을 판정한다. 보완계획은 해결 방향을 제안할 뿐이며, 사용자 또는 팀의 확정 전에는 정책이나 구현 기준이 아니다.
- 검증 또는 구현 중 상위 기준 변경이 필요하면 문제·영향·제안을 검증보고서 또는 보완계획에 기록한다. 영향이 큰 변경은 사용자 또는 팀의 확인을 받은 뒤 새 의사결정 로그 항목으로 확정한다.
- 새 결정에 따라 상위 기준 문서를 갱신하고, 그 새 기준에서 영향받는 하위 명세·설계·구현·검증만 의존성 방향으로 갱신한다. 검증보고서와 보완계획은 새 결정의 입력이 될 수 있으나 기존 정책을 자동으로 변경하거나 기존 정책의 근거가 되지 않는다.

#### 2.1.3 충돌 해결과 완료 점검

- 문서가 충돌하면 `최신 확정 의사결정 로그 → 기획서의 확정 제품 방향 → MVP 명세 → 시스템·데이터 정의 → 인터페이스·기술 설계 → 구현·테스트 → 검증보고서·보완계획` 순으로 판단한다.
- 상위 기준끼리 충돌하거나 어느 문서에도 근거가 없으면 하위 문서에서 임의로 결정하지 않는다. 스크럼 안건 또는 `결정 필요` 항목으로 분리하고, 사용자 또는 팀의 확정 후 의사결정 로그부터 갱신한다.
- 작업 완료 전에는 선행 기준을 확인했는지, 제안 상태의 보완계획을 확정 정책처럼 반영하지 않았는지, 중요한 변경이 사용자 또는 팀 확인과 새 의사결정 로그 항목으로 시작했는지, 판단 근거 관계에 순환 의존성이 없는지를 확인한다.

### 2.2 개발 일지 기록

- 개발일지를 새로 만들거나 갱신할 때는, 그날 확정한 중요한 선택을 실제 진행 순서대로 기록한다.
- 각 선택에는 반드시 `선택`, `이유`, `반영 결과`, `근거`를 함께 쓴다. `근거`에는 의사결정 로그, 관련 문서의 해당 절, 관련 GitHub 커밋 링크를 넣는다.
- 작은 문서 정리·오탈자 수정처럼 결정이 없는 작업은 `작업·검증 기록`에만 남긴다.
- 일지는 작업이 끝난 뒤 같은 날짜 문서에 바로 갱신하고, 비어 있는 날짜 일지는 만들지 않는다.

### 2.3 스크럼 안건 기록

- 스크럼 전에 논의할 안건은 `doc/0. 스크럼 안건/`에 날짜별 `YYYY-MM-DD_스크럼_안건.md` 형식으로 기록한다.
- 안건에는 배경, 논의할 질문, 결정에 따라 반영될 문서를 적는다. 논의 전 제안과 확정된 결정은 구분한다.
- 확정된 중요한 결정은 안건 파일에만 남기지 않고 의사결정 로그와 관련 기획·명세·시스템 문서에 반영한다.
- 안건이 없는 날에는 빈 문서를 만들지 않는다.

### 2.4 보완계획 기록

- 검증보고서의 Pxx 항목 또는 Rxx 위험 항목에 보완계획을 만들 때는 `doc/4. 보완 계획/YYYY-MM-DD/`에 `순번_Pxx_문제명_보완계획.md` 또는 `순번_Rxx_문제명_보완계획.md` 형식으로 작성한다. 날짜 폴더마다 순번은 `1`부터 만들기 순서대로 증가하며, 같은 날짜·같은 식별자의 계획은 한 파일만 둔다.
- 계획 문서에는 `상태: 제안`을 표시하고, `목적과 범위`, `현재 문제 분석`, `보완 목표와 원칙`, `권장 보완안`, `대안과 권장 이유`, `재검증 시나리오와 완료 기준`, `후속 구현에서 정할 항목`, `근거 문서`를 포함한다.
- 제안 단계의 보완계획은 검토 의견이다. 사용자 또는 팀이 확정하기 전에는 기획·MVP·시스템 기준 문서의 정책을 바꾸지 않는다.
- 검증보고서의 해당 Pxx 또는 Rxx 항목과 보완계획 문서는 서로 상대 링크로 연결한다.
- 보완안을 확정하면 의사결정 로그, 관련 기준 문서, 검증보고서 판정을 함께 갱신하고, 같은 날짜 개발 일지에 결정과 검증 결과를 기록한다.

## 3. 마일스톤 검증보고서 규칙

- 각 마일스톤(M0~M5 및 M1.1~M1.3 같은 하위 마일스톤)을 `완료`로 바꾸기 전에 `doc/3. 검증보고서/`에 검증보고서를 작성한다.
- 검증보고서는 `마일스톤 식별자_마일스톤명/` 폴더에 모은다. 예: `M1.1_시스템·데이터_정의/`. 첫 보고서는 `v1_검증보고서.md`로 시작하고, 재검증은 같은 폴더에서 `v2_검증보고서.md`처럼 순번을 올린다. 이 폴더의 `README.md`, 마일스톤 폴더, 검증보고서는 일반 문서 번호 규칙의 예외다.
- 보고서에는 검증 범위, 수행한 확인과 결과, 근거(테스트·화면 확인·문서 링크·커밋), 확인하지 못한 항목과 남은 위험을 기록한다.
- 관련 마일스톤 계획에서 보고서로 연결하고, 의사결정 로그 반영 여부와 `main` 푸시 여부를 함께 확인한다.
- 사용자가 문서·설계·구현의 **검증을 요청**하면, 마일스톤 완료 판정과 관계없이 가장 가까운 마일스톤 또는 페이즈 폴더에 새 검증보고서를 작성한다. 보고서에는 검증 범위, 수행한 확인과 결과, 근거, 미검증 항목과 남은 위험을 기록하고, 아직 완료 조건을 모두 충족하지 않으면 그 사실을 명시한다.
- 같은 대상의 재검증은 기존 마일스톤 폴더에서 보고서 순번을 올린다. 대상 마일스톤이나 페이즈를 식별할 수 없으면, 보고서 작성 전에 사용자에게 범위를 확인한다.

## 4. 의사결정 로그 하네스

기획, 개발, 디자인 작업 중 **중요한 방향 결정**이 확정되면, 관련 구현이나 문서 수정이 끝나기 전에 반드시 `doc/1. 의사결정 로그/`의 해당 날짜 기록에 남긴다. 이 폴더가 확정 결정의 주 기준 위치다.

### 4.1 기록해야 하는 결정

- 프로젝트명·브랜딩·핵심 사용자·핵심 문제의 변경
- 기능의 추가·삭제·핵심 동작 변경
- 사용자 핵심 플로우와 AI의 역할·권한·안전 정책 변경
- MVP 포함/제외, 우선순위, 데모 범위의 변경
- 기술 스택, 아키텍처, 데이터 저장·공개 정책, 외부 서비스·배포 방식의 선택 또는 변경
- 화면 구조나 디자인 원칙을 바꾸는 결정

### 4.2 기록 방법

1. 기존 결정 블록을 지우지 않는다.
2. 결정마다 `DYYYY-MM-DD-NN · 구분 · 상태` 제목을 만들고, `결정`, `이유/근거`, `영향 범위`를 모두 기록한다. `NN`은 해당 날짜 파일 안에서 `01`부터 생성 순서대로 증가한다.
3. 기존 결정을 뒤집으면 기존 결정 블록의 상태를 `폐기`로 바꾸고, 새 결정은 별도 블록으로 남긴다.
4. 단순 오탈자 수정, 문장 다듬기, 코드 포맷팅·리팩터링처럼 제품 방향에 영향이 없는 작업은 기록하지 않는다.
5. 사용자가 명시적으로 “로그에 남기지 말라”고 하면 기록하지 않는다.

날짜별 기록은 `YYYY-MM-DD_의사결정_로그.md` 파일 한 개로 관리한다. 같은 날짜의 결정은 기존 파일에 새 결정 블록으로 추가한다.

### 4.3 완료 조건

중요한 결정이 포함된 작업은 의사결정 로그가 갱신된 뒤에만 완료로 본다. 이 프로젝트에서 변경한 파일은 작업 마무리 시 `main`에 커밋하고 푸시한다.

## 5. 일반 개발 원칙

### 5.1 가정과 판단

- 작은 범위의 되돌릴 수 있는 변경은 합리적인 가정을 밝히고 진행한다.
- 제품 방향, 기술 선택, 데이터·권한, 비용, 보안처럼 결과에 큰 영향을 주는 불확실성은 구현 전에 사용자 또는 팀의 확인을 받는다.
- 더 단순한 구현안이 있으면 우선 검토하고, 의미 있는 트레이드오프만 짧게 설명한다.

### 5.2 최소 변경

- 요청을 해결하는 데 필요한 최소 코드와 파일만 변경한다.
- 요청하지 않은 기능, 단일 사용처를 위한 추상화, 지금 필요하지 않은 설정을 추가하지 않는다.
- 관련 없는 리팩터링·코드 정리·포맷 변경은 하지 않는다. 발견한 문제는 필요한 경우 별도로 알린다.
- 이번 작업으로 사용되지 않게 된 import, 변수, 함수, 파일은 함께 정리한다.

### 5.3 검증

- 작업 성격에 맞는 가장 좁은 검증을 수행한다. 예: 기능 변경은 관련 테스트 또는 직접 동작 확인, 문서 변경은 링크·형식 확인.
- 검증할 수 없으면 그 이유와 남은 위험을 간단히 알린다.
- 마무리 전 변경 사항이 요청 범위를 벗어나지 않았는지 확인한다.
