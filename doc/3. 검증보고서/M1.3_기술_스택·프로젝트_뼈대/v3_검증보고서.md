# M1.3 기술 스택·프로젝트 뼈대 — v3 검증보고서

> 상태: 페이즈 4 재검증 · M1.3 완료 보류 · 2026-09-20

## 검증 범위

- Vercel GitHub 연결, Git 기준 Root Directory, Production 배포 상태와 비로그인 접근
- v2에서 미충족이던 Vercel 관련 항목의 재검증

## 수행한 확인과 결과

| 확인 | 결과 | 근거 |
| --- | --- | --- |
| Vercel Git 연결 | 충족 | Vercel GitHub 앱을 설치하고 `tail1887/hackathon-2person-project`를 `parks-projects-faafd5b8/web`에 연결했다. Git 설정에 `Connected`가 표시된다. |
| Git 기준 Root Directory | 충족 | Vercel Project Settings에서 Root Directory를 `web`으로 저장했다. 저장 성공 안내를 확인했다. |
| Production 배포 | 충족 | `main` 커밋 `a9015b2`를 소스로 한 Production 배포가 `Ready`이며, 배포 URL은 `https://web-five-ivory-44.vercel.app`이다. |
| 비로그인 배포 응답 | 충족 | Vercel Authentication을 해제한 뒤 비로그인 `curl` 요청이 HTTP `200`을 반환했다. |
| 로컬 Supabase 기동 | 미충족 | v2와 동일하게 Supabase CLI가 전역 `/Users/tail1/.supabase/profile`을 읽지 못해 서비스 컨테이너 생성 전에 실패한다. |

## 남은 위험과 재검증 조건

- 다음 `main` 푸시가 새 자동 배포를 만드는지 첫 실제 코드 변경 때 확인한다.
- Supabase CLI 프로필 오류를 복구한 뒤 `supabase start`, `supabase status`, 초기 마이그레이션 적용을 확인한다.
- OAuth·Realtime·RLS·Edge Function·OpenAI 호출은 M2 실제 기능 단위에서 검증한다.

## 완료 판정

- Vercel Git 자동 배포 경로와 외부 공개 접근은 충족했다.
- 로컬 Supabase 기동이 남아 있으므로 M1.3은 **완료 보류**다.
