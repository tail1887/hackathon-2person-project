# M2 대국방 기반 구축 — v8 초대 대기 모델 원격 재검증보고서

> 상태: 부분 검증 · 2026-09-20

## 검증 범위

- D2026-09-20-08의 “수락 시 대국방 확정” 정책을 운영 DB·명령에 반영했는지
- 기존 테스트용 대국방 데이터 정리와 계정·프로필 보존 여부
- 두 Edge Function의 새 명령 배포 및 브라우저 호출 경계

## 수행 결과

| 확인 항목 | 방법 | 결과 | 근거 |
| --- | --- | --- | --- |
| 초대 대기 스키마·원자 명령 | `20260920000500_create_pending_invites.sql`을 운영 SQL Editor에서 실행 | 통과 | `pending_invites`, `create_pending_invite`, `accept_pending_invite` 생성; `rooms.status`는 `active`·`closed`만 허용 |
| 기존 테스트 데이터 정리 | 같은 원격 적용 뒤 개수 조회 | 통과 | `rooms`·`room_members`·`messages`·`pending_invites` 모두 0건 |
| 계정·프로필 보존 | `auth.users`, `profiles` 개수 조회 | 통과 | 로그인 계정 3건, 프로필 3건 유지 |
| 새 함수 명령 배포 | `room-create`, `invite-accept`를 운영 Edge Function에 재배포 | 통과 | 각각 `create_pending_invite`, `accept_pending_invite` 호출 코드 반영 |
| CORS·인증 경계 회귀 | 운영 URL에 `OPTIONS`, 토큰 없는 `POST` 요청 | 통과 | 두 함수 모두 `OPTIONS 200`, 인증 없는 `POST 401` |
| 코드 회귀 검사 | `deno check`, `npm run lint`, `npm run build` | 통과 | 함수 정적 검사와 웹 lint·production build 성공 |

## 판정

초대 발급만으로는 대국방이나 멤버가 생성되지 않는다. 로그인한 수락자가 유효한 링크 또는 코드를 수락할 때만 방·생성자 멤버·수락자 멤버·초대 사용이 하나의 트랜잭션으로 확정되는 구조가 운영 환경에 반영됐다.

## 미검증 항목과 남은 위험

- 서로 다른 A·B 세션으로 초대 발급 뒤 수락했을 때 방과 두 멤버가 생성되는 실제 흐름은 아직 수행하지 않았다.
- 같은 초대의 중복 수락 거절과 제3 사용자의 방 URL·코드 접근 차단은 실제 계정으로 다시 확인해야 한다.
- 페이즈 4의 Realtime 구독·메시지 순서·재연결 복구는 아직 구현 전이다.

## 완료 확인

- [x] 대국방 확정 시점 정책·운영 스키마·명령 반영
- [x] 기존 테스트용 대국방 데이터 정리 및 계정·프로필 보존 확인
- [x] 운영 CORS·인증 경계 재확인
- [ ] A/B/제3 사용자 권한 검증
- [ ] M2 구현·통합 검증 완료

## 관련 기준

- [D2026-09-20-08 대국방 확정 시점](../../1.%20의사결정%20로그/2026-09-20_의사결정_로그.md)
- [M2 페이즈 3 방·초대·권한과 명령](../../7.%20대국방%20기반%20구축/03_방_초대_권한과_명령.md)
- [v7 방·초대 브라우저 호출 재검증보고서](v7_방·초대_브라우저_호출_재검증보고서.md)
