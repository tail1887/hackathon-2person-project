# M1.3 페이즈 4 — 실행과 배포 기본 검증

> 상태: 부분 완료 · 2026-09-20

## 1. 검증 범위

- 빈 Next.js 앱의 로컬 실행·HTTP 응답
- Supabase CLI·Docker 기반 로컬 스택 기동
- Vercel의 `web/` Root Directory 배포와 Git 기반 미리보기 연결
- 비밀값 없이도 앱을 기동할 수 있는지와 OpenAI 키 노출 여부

## 2. 수행 결과

| 대상 | 결과 | 근거 |
| --- | --- | --- |
| 로컬 Next.js | 통과 | `web`에서 `npm run lint`, `npm run build`, `npm run dev`를 실행했다. `http://localhost:3000`은 `200`과 제품 제목·뼈대 안내를 반환했다. |
| 비밀값 없는 기동 | 통과 | `web/.env.example`을 복사한 빈 `.env.local` 없이도 현재 빈 앱이 실행된다. 공개 Supabase 연결값과 서버 전용 OpenAI 키는 분리돼 있다. |
| Docker | 통과 | Docker Desktop을 시작한 뒤 Docker Engine 29.4.0의 응답을 확인했다. 기존 컨테이너는 변경하지 않았다. |
| 로컬 Supabase | 보류 | CLI 2.117.0은 전역 `/Users/tail1/.supabase/profile` 파일을 읽지 못해 서비스 컨테이너 생성 전에 실패했다. `--agent no`로도 같은 오류가 재현됐다. 프로젝트 설정·마이그레이션은 변경되지 않았다. |
| Vercel 배포 | 통과 | `parks-projects-faafd5b8/web` 프로젝트의 Root Directory를 `web`으로 설정했고, `main` 기준 Production 배포 상태가 `Ready`임을 확인했다. |
| Vercel Git 연결·공개 응답 | 통과 | GitHub 앱을 `tail1887/hackathon-2person-project`에 연결했고, Vercel Authentication을 해제한 뒤 배포 URL의 비로그인 HTTP 요청이 `200`을 반환했다. |

## 3. 배포 기록

- 배포 URL: `https://web-cg1ya7xpj-parks-projects-faafd5b8.vercel.app`
- Vercel 프로젝트: `parks-projects-faafd5b8/web`
- 배포 환경: Production
- 배포 빌드: 성공

배포 URL은 데모 확인을 위해 공개 접근으로 설정돼 있다. GitHub의 `main` 푸시는 같은 프로젝트의 자동 배포 대상이다.

## 4. M1.3 완료 전 필요한 외부 설정

1. Supabase CLI 전역 프로필 오류를 복구한 뒤 `supabase start`와 상태 확인을 재실행한다.
2. Supabase 원격 프로젝트·OAuth·OpenAI 비밀값은 M2 실제 기능 추가 시 연결한다. 빈 프로젝트 단계에서 실제 키를 저장소에 넣지 않는다.

## 5. 판정

로컬 빈 앱 실행과 Vercel Git 연결·공개 배포 응답은 확인했다. 로컬 Supabase 기동만 남았으므로 페이즈 4와 M1.3은 부분 완료 상태로 유지한다.
